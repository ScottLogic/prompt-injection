import { CHAT_MESSAGE_TYPE, ChatMessage } from '@src/models/chat';

function ChatBoxMessageWithAvatar({ message }: { message: ChatMessage }) {
	return <div></div>;
}

export default ChatBoxMessageWithAvatar;
